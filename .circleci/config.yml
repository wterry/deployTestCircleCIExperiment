version: 2.1

jobs:
  empaca:
    docker: # run the steps with Docker
      - image: circleci/openjdk:11.0.3-jdk-stretch # ...with this image as the primary container; this is where all `steps` will run
        auth:
          username: wterry
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run:
          name: Empaca
          command: mvn install
      - add_ssh_keys:
          fingerprints:
            - "df:f5:4c:b3:8a:37:e9:b2:e6:95:66:f9:9b:00:8b:d3"
      - run:
          name: Arranca jar
          command: >-
            ssh legosoft@5.189.172.240 -v


workflows:
  maven_manual_deploy:
    jobs:
      - empaca