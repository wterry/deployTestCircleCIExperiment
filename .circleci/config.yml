version: 2.1

jobs:
  empaca:
    docker: # run the steps with Docker
      - image: circleci/openjdk:11.0.3-jdk-stretch # ...with this image as the primary container; this is where all `steps` will run
        auth:
          username: wterry
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run:
          name: Empaca
          command: mvn install
      - run:
          name: Arranca jar
          command: >-
            - ssh legosoft@5.189.172.240 -v


workflows:
  maven_manual_deploy:
    jobs:
      - empaca